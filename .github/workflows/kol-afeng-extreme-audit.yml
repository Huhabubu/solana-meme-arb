name: kol-afeng-extreme-audit

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "research/kol_trading_behavior/KOL阿峰极端路径审计.py"
      - ".github/workflows/kol-afeng-extreme-audit.yml"

permissions:
  contents: read

jobs:
  afeng-extreme-audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Audit Afeng extreme 7-30d price paths
        run: |
          python "research/kol_trading_behavior/KOL阿峰极端路径审计.py" \
            --watchlist "research/kol_trading_behavior/kol_watchlist_afeng.json" \
            --chains 56 \
            --tiers primary \
            --recent-mints 240 \
            --min-first-buy-usd 100 \
            --max-event-age-hours 720 \
            --max-follower-event-age-hours 720 \
            --max-kol-history-rows 5000 \
            --pre-window-seconds 5 \
            --post-window-seconds 300 \
            --entry-window-seconds 16 \
            --max-entry-pages 50 \
            --max-market-pages 100 \
            --workers 3 \
            --output-dir /tmp/kol-afeng-extreme-audit

      - name: Upload audit
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: kol-afeng-extreme-audit-${{ github.sha }}
          path: /tmp/kol-afeng-extreme-audit/
          if-no-files-found: warn
          retention-days: 14
